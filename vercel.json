{
  "buildCommand": "cd avertris-website && npm install && npm run build",
  "outputDirectory": "avertris-website/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
